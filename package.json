{
  "name": "portfolio-parcel",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "version": "1.0.0",
  "description": "portfolio project bundled using parcel.js",
  "scripts": {
    "rebase-env": "rm -rf node_modules",
    "clean": "rm -rf dist",
    "start": "parcel ./src/index.html",
    "build": "yarn clean && parcel build ./src/index.html --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ronny Rahman",
  "license": "ISC",
  "devDependencies": {
    "@glidejs/glide": "^3.5.2",
    "@parcel/transformer-image": "^2.5.0",
    "netlify-plugin-inline-critical-css": "^2.0.0",
    "parcel": "^2.5.0",
    "parcel-plugin-nuke-dist": "^1.0.1",
    "posthtml-include": "^1.7.4",
    "posthtml-obfuscate": "^0.1.5",
    "sharp": "0.29.1"
  }
}
